// dsh-details-tabs — Host half.
//
// This package contributes nothing host-side: it exists so the bundle row is
// present in the loader and the browser half (exports["./client"]) is
// discovered. See lib/client.js for the tabbed details column.
export const name = 'details-tabs'

export function apply() {}
